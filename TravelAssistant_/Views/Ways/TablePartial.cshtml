@model IEnumerable<TravelAssistant_.Models.Way>
<table class="table" data-url="@Url.Action("Index")" id = "elements">
    <thead>
    <tr style="text-align: center">
        <th scope = "col">@Html.DisplayNameFor(model=>model.Id)</th>
        <th scope="col">@Html.DisplayNameFor(model => model.way)</th>
        <th scope="col">@Html.DisplayNameFor(model => model.Length)</th>
        <th scope="col">@Html.DisplayNameFor(model => model.Price)</th>
        <th scope="col">@Html.DisplayNameFor(model => model.Time)</th>
        <th></th><th></th><th scope = "col">Действия</th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr style="text-align: center">
            <th scope="row">@Html.DisplayFor(modelItem => item.Id)</th>
            <td>@Html.DisplayFor(modelItem => item.way)</td>
            <td>@Html.DisplayFor(modelItem => item.Length)</td>
            <td>@Html.DisplayFor(modelItem => item.Price)</td>
            <td>@Html.DisplayFor(modelItem => item.Time)</td>
            <td>
            <td class="text-center">
                <button type="button" class="btn btn-outline-primary btn-sm" 
                        data-toggle="ajax-modal" data-target="#add-way" data-url="@Url.Action("AddOrEdit",new {@id = item.Id})">Редактировать</button>
            </td>
            <td class="text-center">
                <button type="button" class="btn btn-outline-info btn-sm"
                        data-toggle="ajax-modal" data-target="#details-way" 
                        data-url="@Url.Action("Details",new {@id = item.Id})">Подробности</button>
            </td>
            <td class="text-center">
                <button type="button" class="btn btn-outline-danger btn-sm"
                        data-toggle="ajax-modal" data-target="#delete-way" 
                        data-url="@Url.Action("Delete",new {@id = item.Id})">Удалить</button>
            </td>
        </tr>
    }
    </tbody>
</table>